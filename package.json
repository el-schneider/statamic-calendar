{
    "private": true,
    "scripts": {
        "format:check": "prettier --check .",
        "format:fix": "prettier --write ."
    },
    "devDependencies": {
        "prettier": "^3.5.0",
        "prettier-plugin-antlers": "^2.0.5"
    },
    "engines": {
        "node": ">=22.9.0",
        "npm": ">=11.10.0"
    }
}
